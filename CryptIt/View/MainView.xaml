<Window x:Class="CryptIt.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:CryptIt.Converters"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:CryptIt"
        xmlns:cmd ="http://www.galasoft.ch/mvvmlight"
        Title="MainView" Height="500" Width="700">
    <Window.InputBindings>
        <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SendMessageCommand}"></KeyBinding>
    </Window.InputBindings>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBox Text ="{Binding SearchString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="350" />
            </StackPanel>
            <ListView  Grid.Row="1" ItemsSource="{Binding FoundFriends}" SelectedItem="{Binding SelectedUser}">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                                 <GridViewColumn Width="50">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding PhotoUrl}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding FullName}"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Status, UpdateSourceTrigger=PropertyChanged}"/>
                        <GridViewColumn DisplayMemberBinding="{Binding NumberOfNewMessages, UpdateSourceTrigger=PropertyChanged}"/>
                    </GridView.Columns>
                           
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        
           
        <Grid Grid.Column="1">
           <Grid.RowDefinitions>
               <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="15*"></RowDefinition>
                <RowDefinition Height="4*"></RowDefinition>
                <RowDefinition Height="4*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text ="{Binding SelectedUser.FullName}"></TextBlock>
            <ListView 
                Grid.Row="1" ItemsSource="{Binding Messages, UpdateSourceTrigger=PropertyChanged}" >
                
               <ListView.Resources>
                    <converters:UnreadToColorConverter x:Key="UnreadToColorConverter" UnRead="#e5b8d3"/>
                </ListView.Resources>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Background" Value="{Binding IsNotRead, Converter={StaticResource UnreadToColorConverter}}"></Setter>
                                </Style>
                            </StackPanel.Style>
                            <Image Source="{Binding User.PhotoUrl}" />
                            <TextBlock Text="{Binding User.FullName}"/>
                            <TextBlock Text="{Binding Body}"/>
                            <TextBlock Text="{Binding Date}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <i:Interaction.Triggers>
                    <local:RoutedEventTrigger RoutedEvent="ScrollViewer.ScrollChanged">
                        <cmd:EventToCommand Command="{Binding DownloadMessagesCommand}" PassEventArgsToCommand="True"/>
                    </local:RoutedEventTrigger>
                </i:Interaction.Triggers>

            </ListView>
            <TextBox Grid.Row="2" Text="{Binding Message, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
            <StackPanel Grid.Row="3">
                <StackPanel.Resources>
                    <BooleanToVisibilityConverter x:Key ="BooleanToVisibilityConverter"/>
                </StackPanel.Resources>
                <Button  Content="Отправить" Command="{Binding SendMessageCommand}"/>
                <StackPanel Orientation="Horizontal">
                     <TextBlock Width="190" Height="25" Margin="5,10" Text="{Binding FileToUpload.FileName, UpdateSourceTrigger = PropertyChanged}"/>
                    <Button Content="Выбрать файл" Margin="5,10" Width="97" Height="30" Command="{Binding UploadFileCommand}"/>
                    <Image gif:ImageBehavior.AnimatedSource="../Images/loading.gif" Visibility="{Binding IsFileUploading, Converter={StaticResource BooleanToVisibilityConverter}}"></Image>
                </StackPanel>
               
            </StackPanel>
        </Grid>
        
       
    </Grid>
</Window>
