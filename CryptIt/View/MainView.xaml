<Window x:Class="CryptIt.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels ="clr-namespace:CryptIt.ViewModel"
        Title="MainView" Height="350" Width="525">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Grid>

        <WebBrowser x:Name="WebBrowser" Margin="0,0,10,45" Navigated="WebBrowser_OnNavigated">
        </WebBrowser>
        <Button Content="Sign In" HorizontalAlignment="Left" Margin="24,288,0,0" VerticalAlignment="Top" Width="75" Click="Authorize"/>
        <Button Content="GetDialogs" HorizontalAlignment="Left" Margin="144,288,0,0" VerticalAlignment="Top" Width="75" 
                Command="{Binding GetDialogsCommand}"/>
        
    </Grid>
</Window>
